export enum ENV {
  API_URL = 'API_URL'
}

export enum LANGUAGE {
  EN = 'en',
  ES = 'es'
}

export enum ROUTER {
  ABOUT = 'about',
  HEADER = 'header',
  HERO = 'hero',
  NAVBAR = 'navbar'
}

export enum ROUTER_WITHOUT_LANGUAGE {
  social = 'social',
  project = 'project'
}
